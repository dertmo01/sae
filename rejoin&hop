local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")

local LocalPlayer = Players.LocalPlayer
local PlaceId = game.PlaceId

-- ==============================
-- GUI Setup
-- ==============================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PrivateServerBypassGUI"
ScreenGui.ResetOnSpawn = false

if syn and syn.protect_gui then
    syn.protect_gui(ScreenGui)
    ScreenGui.Parent = game.CoreGui
elseif gethui then
    ScreenGui.Parent = gethui()
else
    ScreenGui.Parent = game.CoreGui
end

-- ==============================
-- Frame sizes — easy to tweak
-- ==============================

local BUBBLE_W  = 320
local BUBBLE_H  = 260  -- tall enough for all content
local HEADER_H  = 44

-- ==============================
-- Main bubble frame
-- ==============================

local Bubble = Instance.new("Frame")
Bubble.Name = "ChatBubble"
Bubble.Parent = ScreenGui
Bubble.BackgroundColor3 = Color3.fromRGB(22, 22, 36)
Bubble.BorderSizePixel = 0
Bubble.AnchorPoint = Vector2.new(0.5, 0.5)
Bubble.Position = UDim2.new(0.5, 0, 0.5, 0)  -- dead center
Bubble.Size = UDim2.new(0, BUBBLE_W, 0, 0)    -- starts at 0 for pop-in
Bubble.ClipsDescendants = true
Bubble.Active = true
Bubble.Draggable = true
Instance.new("UICorner", Bubble).CornerRadius = UDim.new(0, 14)

local Stroke = Instance.new("UIStroke", Bubble)
Stroke.Color = Color3.fromRGB(90, 70, 160)
Stroke.Thickness = 1.5
Stroke.Transparency = 0.2

-- ==============================
-- Header bar
-- ==============================

local Header = Instance.new("Frame", Bubble)
Header.Size = UDim2.new(1, 0, 0, HEADER_H)
Header.Position = UDim2.new(0, 0, 0, 0)
Header.BackgroundColor3 = Color3.fromRGB(32, 32, 52)
Header.BorderSizePixel = 0
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 14)

-- square off the bottom of the header so it blends into body
local HFill = Instance.new("Frame", Header)
HFill.Size = UDim2.new(1, 0, 0.5, 0)
HFill.Position = UDim2.new(0, 0, 0.5, 0)
HFill.BackgroundColor3 = Color3.fromRGB(32, 32, 52)
HFill.BorderSizePixel = 0

-- Avatar dot
local Av = Instance.new("Frame", Header)
Av.Size = UDim2.new(0, 28, 0, 28)
Av.Position = UDim2.new(0, 10, 0.5, -14)
Av.BackgroundColor3 = Color3.fromRGB(110, 50, 200)
Av.BorderSizePixel = 0
Instance.new("UICorner", Av).CornerRadius = UDim.new(1, 0)

local AvLbl = Instance.new("TextLabel", Av)
AvLbl.Size = UDim2.new(1, 0, 1, 0)
AvLbl.BackgroundTransparency = 1
AvLbl.Font = Enum.Font.GothamBold
AvLbl.Text = "M"
AvLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
AvLbl.TextSize = 14

-- Title
local TitleLbl = Instance.new("TextLabel", Header)
TitleLbl.Size = UDim2.new(0, 140, 0, 18)
TitleLbl.Position = UDim2.new(0, 46, 0, 6)
TitleLbl.BackgroundTransparency = 1
TitleLbl.Font = Enum.Font.GothamBold
TitleLbl.Text = "Server Bypass"
TitleLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLbl.TextSize = 13
TitleLbl.TextXAlignment = Enum.TextXAlignment.Left

local SubLbl = Instance.new("TextLabel", Header)
SubLbl.Size = UDim2.new(0, 140, 0, 13)
SubLbl.Position = UDim2.new(0, 46, 0, 25)
SubLbl.BackgroundTransparency = 1
SubLbl.Font = Enum.Font.Gotham
SubLbl.Text = "Mochii Scripts"
SubLbl.TextColor3 = Color3.fromRGB(115, 115, 160)
SubLbl.TextSize = 10
SubLbl.TextXAlignment = Enum.TextXAlignment.Left

-- Minimize (—)
local MinBtn = Instance.new("TextButton", Header)
MinBtn.Size = UDim2.new(0, 22, 0, 22)
MinBtn.Position = UDim2.new(1, -52, 0.5, -11)
MinBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 85)
MinBtn.BorderSizePixel = 0
MinBtn.Font = Enum.Font.GothamBold
MinBtn.Text = "—"
MinBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
MinBtn.TextSize = 12
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(1, 0)

-- Close (✕)
local CloseBtn = Instance.new("TextButton", Header)
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Position = UDim2.new(1, -26, 0.5, -11)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.BorderSizePixel = 0
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 11
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(1, 0)

CloseBtn.MouseButton1Click:Connect(function()
    TweenService:Create(Bubble, TweenInfo.new(0.18, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
        Size = UDim2.new(0, BUBBLE_W, 0, 0)
    }):Play()
    task.wait(0.2)
    ScreenGui:Destroy()
end)

local isMin = false
MinBtn.MouseButton1Click:Connect(function()
    isMin = not isMin
    if isMin then
        MinBtn.Text = "+"
        TweenService:Create(Bubble, TweenInfo.new(0.22, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, BUBBLE_W, 0, HEADER_H)
        }):Play()
    else
        MinBtn.Text = "—"
        TweenService:Create(Bubble, TweenInfo.new(0.28, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, BUBBLE_W, 0, BUBBLE_H)
        }):Play()
    end
end)

-- ==============================
-- Body content  (all positions are offsets from top of Bubble)
-- ==============================

-- Player count label
local NowLabel = Instance.new("TextLabel", Bubble)
NowLabel.Position = UDim2.new(0, 12, 0, HEADER_H + 8)
NowLabel.Size = UDim2.new(1, -24, 0, 18)
NowLabel.BackgroundTransparency = 1
NowLabel.Font = Enum.Font.GothamBold
NowLabel.TextXAlignment = Enum.TextXAlignment.Left
NowLabel.TextColor3 = Color3.fromRGB(90, 215, 120)
NowLabel.TextSize = 12

-- Status message bubble
local MsgBg = Instance.new("Frame", Bubble)
MsgBg.Position = UDim2.new(0, 12, 0, HEADER_H + 30)
MsgBg.Size = UDim2.new(1, -24, 0, 30)
MsgBg.BackgroundColor3 = Color3.fromRGB(36, 36, 56)
MsgBg.BorderSizePixel = 0
Instance.new("UICorner", MsgBg).CornerRadius = UDim.new(0, 10)

local StatusLabel = Instance.new("TextLabel", MsgBg)
StatusLabel.Size = UDim2.new(1, -12, 1, 0)
StatusLabel.Position = UDim2.new(0, 8, 0, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 220)
StatusLabel.TextSize = 11
StatusLabel.TextWrapped = true

-- Threshold label
local HintLbl = Instance.new("TextLabel", Bubble)
HintLbl.Position = UDim2.new(0, 12, 0, HEADER_H + 66)
HintLbl.Size = UDim2.new(1, -24, 0, 14)
HintLbl.BackgroundTransparency = 1
HintLbl.Font = Enum.Font.Gotham
HintLbl.TextXAlignment = Enum.TextXAlignment.Left
HintLbl.TextColor3 = Color3.fromRGB(105, 105, 150)
HintLbl.TextSize = 10
HintLbl.Text = "Max players threshold (hop if above):"

-- Threshold input box
local MaxBox = Instance.new("TextBox", Bubble)
MaxBox.Position = UDim2.new(0, 12, 0, HEADER_H + 82)
MaxBox.Size = UDim2.new(1, -24, 0, 28)
MaxBox.BackgroundColor3 = Color3.fromRGB(36, 36, 56)
MaxBox.BorderSizePixel = 0
MaxBox.Font = Enum.Font.GothamBold
MaxBox.Text = "1"
MaxBox.TextColor3 = Color3.fromRGB(255, 255, 255)
MaxBox.TextSize = 13
MaxBox.ClearTextOnFocus = false
Instance.new("UICorner", MaxBox).CornerRadius = UDim.new(0, 8)

-- ==============================
-- Three buttons on one row
-- ==============================

local BTN_Y    = HEADER_H + 118   -- Y offset inside Bubble
local BTN_H    = 36
local BTN_PAD  = 8
local BTN_W    = math.floor((BUBBLE_W - BTN_PAD * 4) / 3)  -- equal thirds

-- Join
local JoinBtn = Instance.new("TextButton", Bubble)
JoinBtn.Position = UDim2.new(0, BTN_PAD, 0, BTN_Y)
JoinBtn.Size = UDim2.new(0, BTN_W, 0, BTN_H)
JoinBtn.BackgroundColor3 = Color3.fromRGB(55, 75, 200)
JoinBtn.BorderSizePixel = 0
JoinBtn.Font = Enum.Font.GothamBold
JoinBtn.Text = "Join"
JoinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
JoinBtn.TextSize = 13
Instance.new("UICorner", JoinBtn).CornerRadius = UDim.new(0, 10)

-- Auto
local AutoBtn = Instance.new("TextButton", Bubble)
AutoBtn.Position = UDim2.new(0, BTN_PAD * 2 + BTN_W, 0, BTN_Y)
AutoBtn.Size = UDim2.new(0, BTN_W, 0, BTN_H)
AutoBtn.BackgroundColor3 = Color3.fromRGB(35, 115, 65)
AutoBtn.BorderSizePixel = 0
AutoBtn.Font = Enum.Font.GothamBold
AutoBtn.Text = "Auto: OFF"
AutoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AutoBtn.TextSize = 12
Instance.new("UICorner", AutoBtn).CornerRadius = UDim.new(0, 10)

-- Rejoin
local RejoinBtn = Instance.new("TextButton", Bubble)
RejoinBtn.Position = UDim2.new(0, BTN_PAD * 3 + BTN_W * 2, 0, BTN_Y)
RejoinBtn.Size = UDim2.new(0, BTN_W, 0, BTN_H)
RejoinBtn.BackgroundColor3 = Color3.fromRGB(115, 45, 185)
RejoinBtn.BorderSizePixel = 0
RejoinBtn.Font = Enum.Font.GothamBold
RejoinBtn.Text = "↩ Rejoin"
RejoinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
RejoinBtn.TextSize = 12
Instance.new("UICorner", RejoinBtn).CornerRadius = UDim.new(0, 10)

-- ==============================
-- Pop-in tween (center → full size)
-- ==============================

TweenService:Create(Bubble, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, BUBBLE_W, 0, BUBBLE_H)
}):Play()

-- ==============================
-- LOGIC
-- ==============================

local autoEnabled = false
local autoThread  = nil

-- Live player count
task.spawn(function()
    while ScreenGui.Parent do
        NowLabel.Text = "● " .. #Players:GetPlayers() .. " player(s) in this server"
        task.wait(1)
    end
end)

local function getRandomServer()
    local url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"
    local ok, raw = pcall(function() return game:HttpGet(url) end)
    if not ok or not raw or raw == "" then return nil end
    local ok2, data = pcall(HttpService.JSONDecode, HttpService, raw)
    if not ok2 or not data or not data.data or #data.data == 0 then return nil end
    local cur = tostring(game.JobId)
    local list = {}
    for _, s in ipairs(data.data) do
        if s.id and tostring(s.id) ~= cur then
            table.insert(list, tostring(s.id))
        end
    end
    if #list == 0 then return nil end
    return list[math.random(1, #list)]
end

local function joinOnce()
    local threshold = math.max(1, math.floor(tonumber(MaxBox.Text) or 1))
    local count = #Players:GetPlayers()
    if count <= threshold then
        StatusLabel.Text = "✓ Only " .. count .. " player(s). No need to hop."
        StatusLabel.TextColor3 = Color3.fromRGB(90, 215, 120)
        return false
    end
    StatusLabel.Text = "Searching for a low-pop server..."
    StatusLabel.TextColor3 = Color3.fromRGB(255, 210, 60)
    task.wait(0.5)
    local serverId = getRandomServer()
    if not serverId then
        StatusLabel.Text = "Can't fetch servers. Enable HTTP in executor."
        StatusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
        return false
    end
    StatusLabel.Text = "Teleporting..."
    StatusLabel.TextColor3 = Color3.fromRGB(120, 180, 255)
    task.wait(0.5)
    local ok, err = pcall(function()
        TeleportService:TeleportToPlaceInstance(PlaceId, serverId, LocalPlayer)
    end)
    if not ok then
        StatusLabel.Text = "Error: " .. tostring(err):sub(1, 55)
        StatusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
        return false
    end
    return true
end

JoinBtn.MouseButton1Click:Connect(function()
    JoinBtn.Active = false
    joinOnce()
    task.wait(2)
    JoinBtn.Active = true
end)

AutoBtn.MouseButton1Click:Connect(function()
    autoEnabled = not autoEnabled
    if autoEnabled then
        AutoBtn.Text = "Auto: ON"
        AutoBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
        autoThread = task.spawn(function()
            while autoEnabled and ScreenGui.Parent do
                local threshold = math.max(1, math.floor(tonumber(MaxBox.Text) or 1))
                local count = #Players:GetPlayers()
                if count <= threshold then
                    StatusLabel.Text = "✓ " .. count .. " player(s). Watching..."
                    StatusLabel.TextColor3 = Color3.fromRGB(90, 215, 120)
                    task.wait(3)
                else
                    joinOnce()
                    task.wait(5)
                end
            end
        end)
    else
        AutoBtn.Text = "Auto: OFF"
        AutoBtn.BackgroundColor3 = Color3.fromRGB(35, 115, 65)
        if autoThread then task.cancel(autoThread) autoThread = nil end
        StatusLabel.Text = "Auto Join stopped."
        StatusLabel.TextColor3 = Color3.fromRGB(180, 180, 200)
    end
end)

-- ==============================
-- REJOIN — leave then come back to the SAME server
-- Flow: save JobId → teleport away → on next load, teleport back to saved JobId
-- Uses TeleportService:GetLocalPlayerTeleportData() to read the saved JobId after load
-- ==============================

RejoinBtn.MouseButton1Click:Connect(function()
    RejoinBtn.Active = false

    local savedJobId = tostring(game.JobId)  -- save THIS server's JobId right now

    StatusLabel.Text = "Leaving server... will rejoin in a moment."
    StatusLabel.TextColor3 = Color3.fromRGB(180, 120, 255)
    task.wait(0.8)

    -- Pass the saved JobId as teleport data so the next load can read it
    local teleportOptions = Instance.new("TeleportOptions")
    teleportOptions:SetTeleportData({ rejoinJobId = savedJobId })
    -- Teleport to a public server of the same place (this causes a real leave)
    local ok, err = pcall(function()
        TeleportService:TeleportAsync(PlaceId, { LocalPlayer }, teleportOptions)
    end)

    if not ok then
        StatusLabel.Text = "Rejoin error: " .. tostring(err):sub(1, 50)
        StatusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
        task.wait(2)
        RejoinBtn.Active = true
        return
    end

    -- After teleport fires, on the NEW server load, read the passed data and jump back
    -- This runs when you land on the temporary server
    task.spawn(function()
        task.wait(3) -- give the teleport time to land
        local data = TeleportService:GetLocalPlayerTeleportData()
        if data and type(data) == "table" and data.rejoinJobId then
            local targetJobId = data.rejoinJobId
            local ok2, err2 = pcall(function()
                TeleportService:TeleportToPlaceInstance(PlaceId, targetJobId, LocalPlayer)
            end)
            if not ok2 then
                warn("Auto-rejoin back failed: " .. tostring(err2))
            end
        end
    end)
end)

-- Init
StatusLabel.Text = "Ready — set threshold and hit Join."
StatusLabel.TextColor3 = Color3.fromRGB(180, 180, 200)
